name: {{ project.name }}-server

services:
  {%- for _, service in project.services.items() %}
{% include 'service_compose.yaml.j2' %}
  {%- endfor %}

networks:
  {{ project.name }}_main_net:
    name: "{{ project.name }}_main_net"
  {{ project.name }}_admin_net:
    name: "{{ project.name }}_admin_net"
